# 题目五：Java FAT 模拟文件系统——项目大纲计划

> 更新：2026-09-23。本文是项目导航；[指导书要求提取](./题目五-指导书要求提取.md)记录原题，[Java 开发方案与阶段任务](./题目五-Java开发方案与阶段任务.md)记录逐项任务和测试，[Java 命名与架构规范](./题目五-Java命名与架构规范.md)记录编码约定。
> 当前状态：需求与方案文档已整理，Java 工程和程序尚未开始实现。

## 1. 项目目标

用 Java 编写一个 Windows 桌面模拟文件系统。宿主机中的一个 8192 字节文件作为模拟磁盘；磁盘包含 128 个 64 字节块，FAT 占第 0、1 块，根目录在第 2 块。程序实现多级目录、绝对路径、文件及目录的 11 项操作、最多 5 项打开文件表，并用两个 64 字节数组完成按需块 I/O。Swing 图形界面展示实际目录、FAT、磁盘块和打开表状态。独立 Shell 使用同一套核心逻辑，并按用户要求支持空闲后重置或退出。

课程要求以 [R01～R16 需求索引](./题目五-指导书要求提取.md#七开发时可直接核对的要求索引)为基线；Shell 超时和工程质量要求是本项目追加内容。早期 [大纲计划](./题目五-模拟磁盘文件系统-大纲计划.md)与 [C++ 命名规范](./FAT模拟文件系统-程序命名与架构规范.md)保留为历史参考，实施时以本套 Java 文档为准。

## 2. 验收范围

| 范围 | 必须展示的结果 | 对应需求 |
| --- | --- | --- |
| 磁盘与 FAT | 新建/挂载镜像；特殊块不参与分配；文件跨块增长和回收正确 | R01～R05、R14 |
| 目录与路径 | 8 字节目录项；每目录 8 项；多级绝对路径；空目录删除 | R06～R07、R13 |
| 文件操作 | 创建、打开、读、追加写、关闭、删除、显示、改属性；5 项打开表 | R08～R12 |
| 交互与测试 | 11 项操作可通过 GUI 触发；CLI 使用相同服务；主函数和测试覆盖边界 | R15～R16 |
| 用户追加 | CLI 默认空闲 600 秒后安全重置，可配置为退出；故障时不报告保存成功 | 用户后续要求 |

验收时需证明数据真实落盘：独立进程完成创建、写入、关闭、退出、重启、读回，并对照 FAT、目录项和块内容。界面截图仅作为可见证据，不能代替镜像与测试结果。

## 3. 技术路线

- 语言基线：Java 21；本机 JDK 25 可用于开发，交付前再用 Java 21 实际运行验证。
- 构建与测试：Maven、Maven Wrapper、JUnit；编译目标固定为 21。
- 图形界面：Swing。磁盘操作在后台串行执行，控件更新返回 Swing 事件分发线程。
- 磁盘 I/O：`FileChannel`，通过两个 `byte[64]` 缓冲槽访问块；镜像独占锁避免两个程序实例同时修改。
- CLI：JLine 输入适配器、可注入单调时钟和可配置空闲策略；脚本输入按批处理完成，不等待交互超时。
- 核心：纯 Java 包分层，GUI、CLI 与测试共同调用 `FileSystemService`，不引入数据库或 Web 服务。

本机检查发现 `java` 当前指向 Java 8，而 `javac`、Maven 使用 JDK 25。开发第一天先统一运行路径，记录在环境说明中。

## 4. 结构和数据流

```text
GUI / CLI / 演示脚本
        ↓
控制器与命令分发
        ↓
文件系统服务 ─ 打开文件表
        ├─ 路径解析 ─ 目录管理
        └─ FAT 分配与链校验
        ↓
双槽块缓冲
        ↓
块设备 ─ data/fatfs.img
```

`model` 与 `error` 包定义通用值对象和错误。虚拟路径 `/usr/a.tx` 只用于模拟文件系统；宿主机路径仅用于定位镜像、日志和脚本。所有文件/目录修改由同一控制器串行提交，成功后刷新镜像，再产生供界面展示的只读快照。

磁盘格式的实施约定在 [开发方案第 5 节](./题目五-Java开发方案与阶段任务.md#5-编码前冻结的数据与行为约定)冻结：`FAT_FREE=0`、`FAT_BAD=254`、`FAT_END=255`，默认示例坏块为 23、49；文件内容以 `#` 结束，目录项第 7 字节是占用块数，打开表中的文件长度按字节计算。原文未规定的名称字符、关闭未打开文件、重复打开模式等行为在该节明确为本项目选择。

## 5. 里程碑

按单人每天约 4～6 小时估算：20 个工作日，加 3 天机动。下表是顺序与完成标志，详细检查项见 [P0～P9 阶段任务](./题目五-Java开发方案与阶段任务.md#7-每个阶段的详细任务)。

| 阶段 | 建议开发日 | 交付结果 | 进入下一阶段前的检查 |
| --- | --- | --- | --- |
| P0 需求与环境 | D1 | 需求和行为约定、统一 JDK、终端输入验证 | Java 版本一致；Swing、JUnit、超时输入冒烟通过 |
| P1 工程与模型 | D2 | Maven 工程、常量、接口、错误码 | 测试入口真实运行；核心不依赖 GUI |
| P2 磁盘与缓冲 | D3～D4 | 64 字节块设备和双缓冲 | 边界块、脏槽、挂载尺寸与重启读回通过 |
| P3 FAT | D5～D6 | 格式化、分配、链校验与回收 | 系统块/坏块跳过；非法链可检测 |
| P4 目录与路径 | D7～D8 | 8 字节目录项、多级路径、`md/dir/rd` | 第 8/9 项、非空删除、重挂载通过 |
| P5 文件生命周期 | D9～D10 | 创建/打开/关闭/删除/改属性、打开表 | 第 5/6 项、权限与失败前置校验通过 |
| P6 文件数据 | D11～D13 | 按字节读取、跨块追加、显示 | 63/64/65 字节、磁盘满和持久化通过 |
| P7 Shell | D14～D15 | 命令解析、脚本、空闲重置/退出 | 真实终端及假时钟边界通过 |
| P8 Swing | D16～D18 | 操作界面和真实状态视图 | GUI/CLI 操作结果一致；窗口可响应 |
| P9 交付 | D19～D20 | 可运行包、完整验收、报告素材 | `clean verify`、独立进程重启读回与演示通过 |
| 机动 | D21～D23 | 修复阻断演示的问题 | 已知限制和测试记录完整 |

## 6. 实施原则与主要风险

1. 先把磁盘格式、命令语义、错误边界定下来，再开始写入镜像；一个格式变更需同步更新测试样例与文档。
2. 每个操作先做能预判的资源与权限检查，再提交修改。真实磁盘 I/O 中途失败可能留下部分更新，程序转入故障状态并停止继续修改；第一版不承诺断电事务恢复。
3. 跨块追加必须为新的 `#` 留空间。长度 63 字节占 1 块，长度 64 字节占 2 块；磁盘满时原文件保持不变。
4. CLI 超时仅在等待命令时生效；正在执行的命令不被打断。重置时完成关闭和刷新，清除打开表及旧输入，不格式化镜像。
5. 目录、FAT、打开表和 GUI 视图都要有可观察的测试证据；以 [T01～T26 测试矩阵](./题目五-Java开发方案与阶段任务.md#8-测试矩阵与完成判定)做最终核对。

## 7. 开发起点与交付清单

第一批编码工作为 P0、P1、P2：统一 JDK；建立 Maven 工程；实现 `DiskLayout`、`BlockDevice` 和 `FileChannelDisk`；让一个 8192 字节镜像能按块写入、关闭、重新打开并读回。随后才接入双缓冲和 FAT。

最终提交 Java 源码、构建配置、启动脚本、说明文档、自动测试、演示记录和课程报告素材。本地 `.skill/`、运行镜像、日志和构建产物保持在 Git 忽略范围内。
