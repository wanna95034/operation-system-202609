# 题目五：Java 开发方案与阶段任务

> 项目：operation-system-202609 / FatFsLab（FAT 模拟文件系统）。
> 日期：2026-09-22。状态：开发规划，尚未实现本方案中的 Java 程序。
> 需求基线：[题目五——指导书要求提取](./题目五-指导书要求提取.md)，对应指导书正文第 17～24 页。
> 用户已确定：使用 Java、采用 FAT、Shell 长时间无操作后自动重置或退出。
> 本文是当前 Java 版实施依据，替代旧大纲与旧架构规范中的 C++、Qt、CMake 等技术选择；磁盘格式与命令语义以指导书要求提取文档为准。

## 1. 交付目标与范围

最终交付一个可以在 Windows 上启动的 Java 桌面程序，用一个 8192 字节二进制文件模拟磁盘，真正实现 FAT 分配、目录检索和文件读写；同一套核心逻辑同时供 GUI、CLI 和测试调用。

| 层次 | 必须完成的内容 | 依据 |
| --- | --- | --- |
| 课程基础功能 | 固定磁盘、FAT、多级定长目录、绝对路径、11 项文件/目录命令、双缓冲、图形界面、主函数测试 | 需求 R01～R16 |
| 用户追加功能 | 独立 Shell；默认空闲 600 秒后安全重置，可改为退出；配置命令和异常处理 | 历次用户要求及既有会话规范 |
| 本项目质量目标 | 正常关闭后重启恢复数据、损坏镜像拒绝挂载、输入校验、自动化测试、错误信息、可演示的内部状态 | 实现方案 |
| 后续增强 | 相对路径、可变长目录、文件中间覆盖写、重命名、拖拽、崩溃恢复日志、安装包美化 | 基础验收完成后再安排 |

第一版先完成可测试的核心，再完成 CLI 与 GUI。界面中的目录树、FAT 表和磁盘块状态必须来自真实磁盘与内存状态，不能用独立的演示数据代替。

## 2. 技术栈与本机环境

### 2.1 推荐技术栈

| 用途 | 方案 | 选择理由 / 约束 |
| --- | --- | --- |
| 开发 JDK | 使用本机已有的 Eclipse Temurin JDK 25，统一 IDE、终端、Maven 的 JDK | 避免多套 Java 混用 |
| 语言与字节码基线 | Java 21，`maven.compiler.release=21`，不使用预览特性 | 便于在 Java 21 及以上环境运行；第三方依赖也须满足此基线 |
| 构建 | Maven 3.9.x + Maven Wrapper；本机检测为 3.9.14 | 一条命令完成编译、测试、打包 |
| GUI | Swing | 可用 `JTree`、`JTable`、对话框实现课程所需界面 |
| 磁盘 I/O | `FileChannel` + 两个 `byte[64]` 块缓冲 | 按块定位读取、写回和刷新 |
| 单元 / 集成测试 | JUnit Jupiter，建议统一到 JUnit BOM 6.1.3 | 参数化测试、临时目录、测试生命周期；依赖版本在 P0 冻结 |
| CLI 终端输入 | JLine 的限时读取能力，封装为 `TerminalInput` | 在等待输入时仍能检测空闲超时 |
| 日志 | JDK `java.util.logging` | 记录命令、错误码、块 I/O 和故障定位信息 |
| 依赖组织 | 单个 Maven 工程，按 Java package 分层 | 规模适合课程设计，便于调试和提交 |

Swing 的组件和界面模型应在事件分发线程（EDT）访问，磁盘操作通过后台执行器完成，再回到 EDT 更新界面。参见 [Java 21 Swing 线程规则](https://docs.oracle.com/en/java/javase/21/docs/api/java.desktop/javax/swing/package-summary.html)。

Maven 的 `release` 设置同时约束语言、字节码和可用 Java SE API；JUnit 6 的运行要求为 Java 17 及以上，适用于本方案。参见 [Compiler Plugin 的 release 配置](https://maven.apache.org/plugins/maven-compiler-plugin/examples/set-compiler-release.html) 与 [JUnit 6.1.3 指南](https://docs.junit.org/6.1.3/overview.html)。

课程项目以本地 Java 桌面应用组织。第一版不需要数据库、Web 服务、ORM 或依赖注入框架；对象由启动类通过构造器组装即可。

### 2.2 已检查的本机现状

2026-09-22 的只读检查结果：

| 命令 | 实际结果 |
| --- | --- |
| `java -version` | Java `1.8.0_481`，来自 Oracle `java8path` |
| `javac -version` | `25.0.1` |
| `mvn -version` | Maven `3.9.14`，使用 Eclipse Adoptium JDK `25.0.1` |

当前 `java` 与编译环境不一致。P0 必须先统一运行路径，否则会发生编译成功但启动时字节码版本不兼容。此次仅检查并记录，没有修改系统环境变量。

Maven 版本检查曾先输出一行 `Access is denied.`，随后正常显示版本；P0 的实际构建冒烟测试需要确认其来源，不能把版本信息可见当作完整构建已通过。

最低运行环境拟定为 Java 21；最终交付前须在 Java 21 环境实际启动测试一次。构建参数不能替代运行验证。

## 3. 项目结构与 Java 命名规范

本节列出实施时最常用的约定；完整类名、接口边界与文件命名见 [Java 命名与架构规范](./题目五-Java命名与架构规范.md)。

### 3.1 工程标识

| 对象 | 规划名称 |
| --- | --- |
| Git 仓库 | `operation-system-202609` |
| Maven `groupId` | `edu.oslab` |
| Maven `artifactId` | `fatfs-lab` |
| 根包 | `edu.oslab.fatfs` |
| 启动类 | `FatFsApplication` |
| 开发版 JAR | `fatfs-lab-1.0.0.jar` |
| 默认镜像 | `data/fatfs.img` |
| 日志目录 | `logs/` |

### 3.2 建议目录

以下是后续编码时建立的目录；当前交付只包含规划文档。

```text
operation-system-202609/
├─ pom.xml
├─ mvnw / mvnw.cmd
├─ .mvn/wrapper/
├─ src/
│  ├─ main/
│  │  ├─ java/edu/oslab/fatfs/
│  │  │  ├─ bootstrap/    FatFsApplication、对象组装、参数解析
│  │  │  ├─ model/        常量、目录项、文件指针、枚举、结果快照
│  │  │  ├─ error/        FsErrorCode、FsException
│  │  │  ├─ storage/      BlockDevice、FileChannelDisk
│  │  │  ├─ buffer/       BlockBufferPool、BufferSlot
│  │  │  ├─ fat/          FatTable、FatChain
│  │  │  ├─ directory/    DirectoryEntryCodec、DirectoryManager
│  │  │  ├─ path/         FileName、VirtualPath、PathResolver
│  │  │  ├─ openfile/     OpenFileTable、OpenFileEntry
│  │  │  ├─ fs/           FileSystemService、DefaultFileSystemService、ConsistencyChecker
│  │  │  ├─ app/          Command、CommandDispatcher、FsController
│  │  │  ├─ cli/          ShellSession、TerminalInput、IdlePolicy
│  │  │  └─ ui/           MainFrame、树/表模型、操作对话框
│  │  └─ resources/       默认配置、日志配置
│  └─ test/
│     ├─ java/edu/oslab/fatfs/  与主代码对应的测试
│     └─ resources/           小型命令脚本、损坏镜像测试描述
├─ docs/                     磁盘格式、测试记录、演示步骤、报告资料
├─ scripts/                  run-gui.cmd、run-cli.cmd
├─ data/                     本地运行镜像
├─ logs/                     本地运行日志
├─ target/                   Maven 构建产物
└─ .skill/                   已安装的本地 ECC，继续被 Git 忽略
```

### 3.3 编码规则

- 包名小写；类、接口、record 使用 `PascalCase`；字段和方法使用 `camelCase`；常量使用 `UPPER_SNAKE_CASE`。
- 一份 `.java` 文件放一个公开顶层类型；采用 4 空格缩进。
- 值对象与返回快照优先不可变；含数组的 record 需要防御性复制，不能只靠 record 获得不可变性。
- 通过构造器注入依赖；不使用全局可变的 FAT、打开文件表或磁盘对象。
- 资源通过 `AutoCloseable` 和明确的生命周期管理；关闭错误不能被吞掉。
- `FsException` 携带 `FsErrorCode` 和必要上下文，由应用层统一转换为中文提示；I/O 故障保留原始异常原因。
- “文件不存在”等正常失败和“磁盘写入失败”等基础设施故障必须区分，后者可能要求停止后续修改。
- 测试类用 `<对象>Test`；集成测试用 `<场景>IT`；测试方法描述条件和结果，例如 `append_whenCrossingBlock_updatesFatChain`。

以上约定参考已安装 ECC 的 `java-coding-standards` 中通用 Java 部分，并按桌面文件系统项目调整。

## 4. 架构与职责

### 4.1 调用方向

```text
Swing GUI / CLI / 演示主函数
           ↓
FsController / CommandDispatcher
           ↓
FileSystemService
    ├─ OpenFileTable
    ├─ PathResolver → DirectoryManager
    └─ FatTable / FatChain
           ↓
BlockBufferPool：两个 64 字节槽
           ↓
BlockDevice → FileChannelDisk
           ↓
data/fatfs.img
```

`model`、`error` 是共享基础类型，不反向依赖上层。只有 `FileChannelDisk` 操作宿主机镜像文件。虚拟路径 `/usr/a.tx` 不得交给宿主机 `Files.createFile()` 等 API 实现。

### 4.2 模块交付清单

| 模块 | 主要职责 | 禁止承担的职责 |
| --- | --- | --- |
| `FileChannelDisk` | 新建/挂载镜像、完整块读写、尺寸检查、刷新、镜像锁 | 路径解析、FAT 分配 |
| `BlockBufferPool` | 维护两个缓冲槽的块号、有效位、脏位和占用状态 | 文件权限和命令解析 |
| `FatTable/FatChain` | 加载 FAT、选择空闲块、连接、校验、映射、回收链 | 直接操作 UI |
| `DirectoryEntryCodec` | 8 字节目录项的显式编码和解码 | Java 对象序列化 |
| `DirectoryManager` | 单块目录查找、插入、更新、删除、判空 | 隐式创建父目录 |
| `PathResolver` | 从根目录逐级解析绝对路径，返回父目录和槽位 | 修改镜像 |
| `OpenFileTable` | 最多 5 项数组、模式、长度、读写位置、查找/移除 | 磁盘文件句柄管理 |
| `FileSystemService` / `DefaultFileSystemService` | 定义并实现 11 项操作、前置校验和提交顺序 | Swing 组件访问 |
| `CommandDispatcher` | 保留指导书的路径命令语义，调用核心服务 | 重复编写 FAT 算法 |
| `ShellSession` | 输入、空闲策略、安全重置与退出 | 从计时线程直接修改 FAT |
| `FsController` | 串行提交操作、返回只读视图快照 | 用 UI 状态替代真实文件系统状态 |
| `ConsistencyChecker` | 检查保留块、链环、交叉占用、长度与引用 | 自动格式化或擅自修复损坏镜像 |

### 4.3 命令与 Java 方法

外部仍按文件绝对路径操作。内部可以使用表槽号优化定位，但不能丢失读写时自动打开的行为。

| 命令 | 建议方法 | 关键行为 |
| --- | --- | --- |
| `create_file` | `createFile(path, attributes)` | 预检资源，创建后按写方式加入打开表 |
| `open_file` | `openFile(path, mode)` | 同模式重复打开不增加表项 |
| `read_file` | `readFile(path, length)` | 未打开时按读方式打开；返回原始字节及 EOF 状态 |
| `write_file` | `writeFile(path, data, length)` | 未打开时按写方式打开；从尾部追加 |
| `close_file` | `closeFile(path)` | 未打开则返回“无需关闭”；写文件关闭时确保结束符并刷新 |
| `delete_file` | `deleteFile(path)` | 已打开则拒绝；回收整条链 |
| `typefile` | `typeFile(path)` | 已打开则拒绝；不占用打开表项 |
| `change` | `changeAttributes(path, attributes)` | 已打开则拒绝 |
| `md` | `makeDirectory(path)` | 父目录存在，申请一块并初始化空项 |
| `dir` | `listDirectory(path)` | 返回目录项快照 |
| `rd` | `removeDirectory(path)` | 只允许空子目录；保护根目录 |

重复打开时若请求模式与已打开模式不同，第一版返回 `OPEN_MODE_CONFLICT`，提示先关闭再打开；原文未规定模式切换，故这是本项目约定。

### 4.4 并发和界面更新

- 一个挂载的镜像只由一个核心串行执行器处理操作，避免共享双缓冲和 FAT 的竞态。
- GUI 按钮和 GUI 内命令框通过同一控制器排队；等待时显示执行状态并防止重复提交。
- 所有 Swing 组件和 `TableModel` 更新回到 EDT；后台只生成快照。
- CLI 模式和 GUI 模式第一版择一启动；使用镜像独占锁拒绝第二个本程序实例同时挂载同一镜像。
- 快照可保存 FAT 数值、目录项和打开表信息；磁盘原始内容查看只加载选中的块，不建立整盘字节缓存。
- 后台操作、GUI 响应已能展示并行发生的行为；核心写操作仍按顺序完成。

文件锁用于协调遵守同一锁协议的进程，不能替代 JVM 内线程调度。参见 [Java FileLock 说明](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/nio/channels/FileLock.html)。

## 5. 编码前冻结的数据与行为约定

本节是项目设计；原文未明确的细节在此作出可实施的选择。

### 5.1 磁盘与目录编码

- `BLOCK_COUNT=128`，`BLOCK_SIZE=64`，`IMAGE_SIZE=8192`。
- 第 0、1 块存 FAT，第 2 块存根目录；第 0～2 项初始化为保留占用值 `255`，分配时仍显式跳过这三块。
- `FAT_FREE=0`，`FAT_BAD=254`，`FAT_END=255`。
- 默认课程演示镜像按指导书示例将第 23、49 块标坏；无坏块配置仅用于独立测试。默认可分配块数为 `128-3-2=123`，无坏块时为 `125`。
- FAT 在内存允许保留 128 字节的元数据表；它的装载和写回仍必须经过两个 64 字节缓冲。不得保留完整 8192 字节镜像副本。
- 每个目录恰好一块、8 个槽位；不建立额外的 `.`、`..` 项占用容量。
- 文件目录项第 7 字节写占用块数；子目录该字节写数值 `0`，即使目录物理上占一块。
- 空项第一个字节写 `$`；短名称和扩展名用空格补足字段，其他无效字节初始化清零。
- 文件/目录编码均为明确的 8 字节格式，禁止使用 `ObjectOutputStream`、`writeUTF()` 或 Java 对象内存布局代替。
- Java `byte` 是有符号类型，解码 FAT、块号、块数、属性时用 `Byte.toUnsignedInt()`；计算和比较使用 `int`，最后写盘才转换为 `byte`。

### 5.2 名称、路径和属性

- 文件基本名 1～3 字节，扩展名 0～2 字节；目录名 1～3 字节。
- 名称采用 US-ASCII 的非空格可打印字符，排除 `$`、`.`、`/`；`.` 仅作文件类型分隔符。示例 `a-b.tx`、`x_y.t` 合法。这样保留原文允许的部分其他字符，而不收紧成只允许字母数字。
- 模拟名称不支持中文；宿主机镜像所在目录可以是中文路径，二者分别测试。
- 名称比较区分大小写；同目录中使用同一显示名称的文件和目录不得冲突。
- 路径必须以 `/` 开头；根目录为 `/`；连续分隔符统一规范化；拒绝相对路径和 `.`、`..` 段。
- 采用 Java `String` 保存完整虚拟路径，不继承 C 示例 `name[20]` 的固定数组容量；每段长度受目录格式限制。
- 提供文件属性预设：`4` 普通读写、`1` 普通只读、`2` 系统读写、`3` 系统只读；目录单独使用 `8`。创建文件只接受 `2/4`，关闭后可改为 `1/2/3/4`。
- 其他位组合第一版拒绝，以避免“可读写普通文件”和“只读”等组合的歧义；系统文件标记不额外引入原文未要求的权限模型。

### 5.3 内容、长度和结束符

- 核心 API 处理 `byte[]`，长度与读写位置全部按字节计算。
- 界面和 CLI 将文本按 UTF-8 编码；写长度使用编码后的数组长度，不能使用 `String.length()`。
- 内容保留字节 `0x23`（`#`）作为结束符，写入前拒绝包含该字节的数据。
- 创建空文件立即写入 `#`；每次成功追加后维持有效结束符；关闭时再次确保结束符、目录块数和刷新状态一致。
- 文件字节长度不包含 `#`；目录项占用块数包含保存 `#` 所需的块。
- 数据长度为 `L` 时，需要 `max(1, ceil((L+1)/64))` 个块。例如 63 字节内容占 1 块，64 字节内容占 2 块。
- 打开已有文件时沿链查找 `#`，恢复实际字节长度和尾部写位置；没有结束符不能直接假定文件正常。
- 部分读取可能截断 UTF-8 字符。核心照常返回请求的原始字节；界面提供十六进制查看，对不完整文本作明确提示，不能擅自移动指针“补齐字符”。`typefile` 读取完整内容后统一解码。

### 5.4 两个缓冲与完整块 I/O

- 缓冲池固定两槽，每槽一个 `byte[64]`，记录 `blockNumber`、`valid`、`dirty`、`pinCount`。
- 槽位被使用时不能被替换；替换脏槽前先写回；两个槽均占用时返回明确错误，不临时新增第三个块缓存。
- 目录或数据修改采用读块、修改块内字段、整块写回；尾块剩余部分补零，实际有效长度仍由 `#` 决定。
- 将 Java `ByteBuffer` 包装在现有 64 字节数组上，用位置式读写；处理短读、短写和零进展，不能假定一次 `read/write` 就完成 64 字节。
- 初始化磁盘也逐块写入；FAT 两个块分别装载/刷新。
- 输出数据、目录项对象和 FAT 元数据不是额外磁盘块缓存，但不得以它们为名绕过按需块 I/O。
- 释放缓冲引用后才能执行可能引发其他读块的操作；关键流程预先设计同时占用的槽位数量。

`FileChannel` 提供位置式读写和 `force()`；刷新宿主机文件也不能把多个块的修改自动变成原子事务。参见 [Java 21 FileChannel](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/nio/channels/FileChannel.html)。

### 5.5 失败处理与持久化边界

1. **可以事先判定的错误**：非法名称、目录满、打开表满、权限不符、磁盘空间不足，都必须在修改磁盘前判定。失败后磁盘内容与打开表保持原样。
2. **追加预检**：根据旧长度、追加字节数及新的 `#` 计算完整需求，选定全部新增块后再提交；不采用磁盘写满了才发现没有位置放 `#` 的方式。
3. **提交顺序**：先初始化新增块并写数据，再提交 FAT 和目录信息；同一命令串行完成。每次成功的修改命令完成数据、FAT、目录的写回及刷新，再向用户报告成功。
4. **真实 I/O 故障**：写入中途失败可能留下部分更新。进入 `FAULTED`，保留未完成状态和错误信息，拒绝后续修改；不得只恢复新增 FAT 块就声称完整回滚。
5. **重启检查**：存在的镜像只挂载，不因启动或超时重置而重新初始化。镜像尺寸错误、链异常等明确报错，不自动格式化。
6. **保证范围**：第一版保证正常完成、关闭和重启时的持久化，以及可预检失败不产生修改。断电/进程强杀后的事务恢复不在第一版保证内；如要保证，需另设日志或写前记录阶段。

这也修正了旧规范“任何失败都能回滚”的过强表述：永久磁盘故障下，普通文件 I/O 不能保证回滚本身成功。

## 6. 阶段总览与时间安排

按单人、每天约 4～6 小时有效开发估算为 **20 个工作日 + 3 天机动时间**。如果 Java 或二进制文件操作基础较弱，可按每阶段实际进度延长。以下日期为相对开发日，不是固定截止日期。

| 阶段 | 建议时间 | 依赖 | 阶段成果 |
| --- | --- | --- | --- |
| P0 需求与环境 | D1 | 无 | 规则冻结、JDK 统一、终端技术验证 |
| P1 工程骨架与模型 | D2 | P0 | Maven 工程、公共模型、错误码、测试入口 |
| P2 磁盘与双缓冲 | D3～D4 | P1 | 可可靠读写 64 字节块的底座 |
| P3 FAT 与格式化 | D5～D6 | P2 | 可分配、映射和回收的 FAT |
| P4 目录与绝对路径 | D7～D8 | P3 | `md/dir/rd` 和多级目录检索 |
| P5 打开表与文件生命周期 | D9～D10 | P4 | 创建、打开、关闭、删除、改属性 |
| P6 文件读取与追加 | D11～D13 | P5 | 跨块读写、结束符、磁盘满预检 |
| P7 CLI 与空闲超时 | D14～D15 | P6 | 全部命令、脚本模式、安全重置/退出 |
| P8 Swing 图形界面 | D16～D18 | P6；集成命令框依赖 P7 | 可操作、可观察真实状态的 GUI |
| P9 集成验收与交付 | D19～D20 | P7、P8 | 可运行包、验收记录、报告素材 |
| 机动 | D21～D23 | 按缺陷情况 | 环境兼容、边界问题、演示排练 |

GUI 布局草图可提前准备，连接真实文件系统应等核心读写通过测试。每一阶段完成后再进入依赖它的阶段。

## 7. 每个阶段的详细任务

### P0：确认规则与统一环境

**目标：** 固定行为约定，确保代码写完后能够在同一 Java 环境运行。

任务：

- [x] P0-01 提取题目五原文要求，建立 R01～R16 索引并标记歧义。
- [ ] P0-02 阅读本文第 5 节，冻结坏块示例、名称编码、属性组合、重复打开、关闭未打开文件、结束符占块规则。
- [ ] P0-03 统一 `JAVA_HOME`、当前终端 PATH、IDE Project SDK 与 Maven Runner JDK；将项目启动脚本绑定到该 JDK，验证 `java/javac/mvn` 的结果一致。
- [ ] P0-04 固定 Maven Wrapper、编译插件、测试插件、JUnit 和 JLine 版本；编译目标 21，验证依赖也可在 Java 21 使用。
- [ ] P0-05 做最小 Swing 窗口与 JUnit 测试冒烟，确认中文项目路径可以构建和运行。
- [ ] P0-06 做 JLine 技术验证：在 Windows Terminal 中不输入时仍能每 200ms 返回检查；可识别换行、退格和 EOF；确认所选终端 provider 所需依赖/启动参数。
- [ ] P0-07 记录 GUI、独立终端、脚本输入三种运行方式；不将 IDE 内置控制台表现等同于真实终端表现。

**交付物：** `docs/environment.md`、`docs/disk-format.md` 初稿、版本表、终端冒烟记录。

**验收：** 同一终端编译和启动无版本错误；窗口能显示；至少一个 JUnit 测试实际被执行；空闲期间输入适配器可返回。没有必要在此阶段安装尚未选定的 GUI 美化或数据库依赖。

### P1：搭建工程和公共模型

**目标：** 让后续模块有稳定接口和统一错误表达。

任务：

- [ ] P1-01 建立第 3 节的 Maven 目录，配置 UTF-8、编译目标和可运行启动类。
- [ ] P1-02 定义 `DiskLayout`、FAT 特殊值、最大打开数、每目录槽数等常量。
- [ ] P1-03 定义 `DirectoryEntry`、`EntryLocation`、`FileCursor`、`OpenMode`、`FileAttributes` 等模型；起始块和属性使用经过范围检查的 `int`。
- [ ] P1-04 定义 `FsErrorCode`：路径/名称无效、未找到、重名、目录满、磁盘满、表满、模式冲突、文件忙、损坏链、I/O 失败等。
- [ ] P1-05 定义 `FsException` 与成功返回对象，保证应用层能区分“无需关闭”和真正失败。
- [ ] P1-06 定义 `BlockDevice`、`TerminalInput`、单调计时源接口；准备测试替身，避免后续测试绑定真实控制台和真实等待。
- [ ] P1-07 配置 JUnit 单元测试与 Failsafe 集成测试命名，固定 Surefire/Failsafe 兼容版本；`verify` 必须实际运行 `*IT`，不能只编译它们。
- [ ] P1-08 在后续代码开发时补充 Java 构建目录忽略：`target/`、`logs/`、本地镜像；保留现有 `.skill/` 与发现链接的忽略规则。

**交付物：** `pom.xml`、启动骨架、模型与接口、基础测试。

**验收：** `mvnw.cmd test` 能运行测试；没有核心包依赖 Swing；Git 未把本地 skill、日志和运行镜像加入待提交列表。

### P2：实现模拟磁盘与两个块缓冲

**目标：** 在不整盘载入的前提下，准确访问任一 64 字节块。

任务：

- [ ] P2-01 实现新建镜像和挂载已有镜像两条路径，初始化逐块写入 8192 字节。
- [ ] P2-02 实现块号与文件偏移换算 `offset = blockNumber * 64L`，拒绝 `-1/128` 等越界块号。
- [ ] P2-03 实现完整块读写循环；正数短读继续读，未收齐 64 字节就遇 EOF 才报镜像损坏；短写继续直至完成或失败，连续零进展要有错误出口。
- [ ] P2-04 封装镜像独占锁、`flush/force` 和安全关闭，防止本程序另一实例同时修改镜像。
- [ ] P2-05 实现两个缓冲槽的命中、空槽使用、简单替换、脏块写回和占用计数。
- [ ] P2-06 为 FAT、目录和数据统一提供缓冲访问方法；调用者结束使用后释放槽位，释放失败也要可追踪。
- [ ] P2-07 增加可观察的块读写事件，供测试核对每次物理 I/O 的块号、长度与顺序。
- [ ] P2-08 测试块 0、2、127，短文件/超长文件，两个脏槽替换，全部槽被占用，以及关闭后重新打开。

**交付物：** `FileChannelDisk`、`BlockBufferPool`、磁盘/缓冲测试、块 I/O 日志示例。

**验收：** 读写不影响相邻块；镜像大小始终正确；脏槽写回后重开可读到内容；不存在整盘 `readAllBytes()`、整盘内存映射或额外常驻块缓存。

### P3：实现 FAT、链操作与格式化

**目标：** 所有分配和回收都经 FAT，特殊块不能混入文件链。

任务：

- [ ] P3-01 经双缓冲装载和写回 FAT 的两个块，确认 `254/255` 无符号解码正确。
- [ ] P3-02 完成首次格式化：保留 0～2 块、标记示例坏块、清空数据区、将根目录 8 项首字节写 `$`。
- [ ] P3-03 实现从第 3 块开始的空闲搜索，提供分配单块、预选 N 块和统计空闲数的方法。
- [ ] P3-04 实现后继查询、链尾标记、连接新块、逻辑块号到物理块号映射。
- [ ] P3-05 实现链收集与校验：越界、系统区、空闲项、坏块、环都必须被检测；遍历数量受 128 块上限约束。
- [ ] P3-06 完整校验链后才能回收，不边遍历边破坏尚未验证的链。
- [ ] P3-07 编写确定性测试：正常链、非连续链、坏块跳过、分配耗尽、环、无效后继、回收后再分配。
- [ ] P3-08 建立格式化与挂载的边界：已有镜像只读取校验；用户显式格式化才重建内容。

**交付物：** `FatTable`、`FatChain`、格式化流程、FAT 测试和初始布局记录。

**验收：** 默认新镜像可分配 123 块，23/49 不会返回；第 0～2 块永不分配；释放后空闲数正确；非法链及时失败，无死循环。

### P4：目录项、多级目录与路径

**目标：** 形成可按绝对路径访问的真实目录结构。

任务：

- [ ] P4-01 编写 8 字节 `DirectoryEntryCodec`；分别处理文件项、目录项与空项，不依赖 Java 字符或对象大小。
- [ ] P4-02 编写逐字节黄金样例测试，验证名称填充、属性、起始块、文件块数、目录保留字节为 0。
- [ ] P4-03 实现目录项查找、空槽搜索、插入、更新、删除和判空；每次修改整块写回。
- [ ] P4-04 实现 `VirtualPath`、`FileName` 校验与统一规范化，区分根路径、父路径和最后一段名称。
- [ ] P4-05 实现 `PathResolver.resolve()` 与 `resolveParent()`；路径中间段必须为目录；没有父目录时不隐式建立。
- [ ] P4-06 实现 `md/dir/rd`，创建目录先检查重名、空槽和空闲块，初始化新块后登记父项。
- [ ] P4-07 测试 `/a/b/c` 等多级结构；每级 8 项容量独立；第 9 项创建失败且不消耗磁盘块。
- [ ] P4-08 测试根目录保护、非空目录不可删、删除空子目录后回收块与父项、非法名称及文件占据中间段的情况。

**交付物：** 目录编解码、目录管理、路径解析、三个目录操作及对应测试。

**验收：** 目录操作已真实落盘，重挂载后层级不变；8 项限制严格；目录槽位和磁盘空间均可复用。

### P5：打开文件表与文件生命周期

**目标：** 建立创建、打开、关闭、删除与属性修改的完整状态约束。

任务：

- [ ] P5-01 用长度为 5 的 `OpenFileEntry[]` 实现打开表，支持按规范化路径查询、插入、删除和只读快照。
- [ ] P5-02 保存文件属性、起始块、实际字节长度、读写模式及读写逻辑偏移；展示时换算为物理块号和块内位置。
- [ ] P5-03 实现创建前置校验：父目录、重名、属性、目录空槽、打开表容量和空闲块都满足后，才修改镜像。
- [ ] P5-04 创建文件时初始化 `#`，登记一块长度，并以写方式加入打开表。任一可预检条件失败都不能留下孤立块。
- [ ] P5-05 实现文件长度扫描辅助方法，沿 FAT 查找 `#`；实现读模式打开及写模式打开的指针初始化。
- [ ] P5-06 实现重复打开规则、只读写入限制及读写模式冲突；第 6 项拒绝加入。
- [ ] P5-07 实现关闭：未打开时返回无需关闭；已打开时按模式处理，刷新成功后移除表项。此阶段先验证空文件关闭，P6 补齐任意长度关闭测试。
- [ ] P5-08 实现删除与修改属性：先检查打开表，已打开文件拒绝；删除前完整验证 FAT 链。
- [ ] P5-09 明确关闭单个文件、关闭全部文件和卸载的关系；只有成功关闭的表项才能删除，失败项保留供诊断和重试。

**交付物：** `OpenFileTable`、文件生命周期服务、长度扫描方法、状态和权限测试。

**验收：** 创建空文件后表项与目录一致；最多打开 5 个；第 6 次创建失败不改变目录或 FAT；关闭后槽位可重用；已打开文件不能删除或改属性。`typefile` 的占用检查在 P6 验收。

### P6：按字节读取、跨块追加与文件显示

**目标：** 完成题目最核心的数据路径，能够正确处理边界和空间不足。

任务：

- [ ] P6-01 实现按路径自动打开：`read_file` 默认读方式，`write_file` 默认写方式；已有打开项保留原模式并检查冲突。
- [ ] P6-02 实现读取：按逻辑偏移映射物理块，从块内偏移读取，跨 FAT 链继续，遇 `#` 或满足请求长度停止。
- [ ] P6-03 每次成功读取仅推进实际读取的字节数；再次读取 EOF 返回空数据和 EOF 状态，不越过链尾。
- [ ] P6-04 实现追加需求计算，用足够宽的整数计算 `旧长度 + 追加长度 + 1`；验证写长度不负、不超过输入数组长度、不发生溢出。
- [ ] P6-05 在写入前确定所需新增块数和打开表资源；没有空间时整次拒绝，不改变旧内容、旧 `#`、目录项、指针或打开表。
- [ ] P6-06 覆盖旧结束符，利用原尾块剩余空间，必要时连接新块，写入新的 `#`，更新长度、指针和目录块数。
- [ ] P6-07 尾块采用完整块写回；成功命令结束前写回数据、FAT、目录并刷新镜像。
- [ ] P6-08 实现 `typefile`：拒绝已打开文件，直接通过核心逐块读取到 `#`，不隐式占用打开表。
- [ ] P6-09 对原始 `byte[]` 与文本显示分层；UTF-8 编解码错误可定位，部分读取始终允许观察原始字节。
- [ ] P6-10 使用参数化测试覆盖 `0/1/62/63/64/65/127/128/129` 字节，以及同一文件的多次小追加。
- [ ] P6-11 注入第 N 次写块失败，验证不误报成功、进入故障状态、后续修改被拒绝且错误包含操作和块号。
- [ ] P6-12 新建新的文件系统对象并重新挂载，复核内容、字节长度、FAT 链和目录项块数。

**交付物：** 文件读写与显示实现、长度边界测试、磁盘满测试、持久化与故障测试。

**验收：** 空文件读到 EOF；63 字节内容用一块，64 字节内容用两块；先写 63 字节再追加 1 字节结果正确；磁盘仅剩不足以保存新结束符的空间时，旧文件完整不变。

P6 结束时，11 项文件/目录功能应能通过服务接口直接完成。CLI 和 GUI 后续只增加交互，不重新实现读写算法。

### P7：CLI 命令、空闲重置与安全退出

**目标：** 提供可操作、可脚本测试，并满足用户空闲超时要求的 Shell。

任务：

- [ ] P7-01 实现命令解析，覆盖全部 11 项命令、参数个数、数值范围、引号和转义；错误命令给出用法提示。
- [ ] P7-02 实现 `help`、`exit`、状态查询及 `--script` 批处理入口；批处理出错返回非零状态码并指出行号，输入结束后安全清理。
- [ ] P7-03 在 `TerminalInput` 中使用 JLine 限时字符读取，组装行缓冲；保存并在退出时恢复终端模式，处理退格、换行、EOF 和中断信号。
- [ ] P7-04 将单调计时源注入 `ShellSession`；生产使用 `System.nanoTime()`，测试使用可推进的假时钟。
- [ ] P7-05 实现默认 `600` 秒空闲、`200ms` 检查、允许 `60..3600` 秒，动作默认为 `reset`，可设为 `exit`。
- [ ] P7-06 实现 `timeout status`、`timeout set <seconds>`、`timeout action reset|exit`、`timeout reset`。
- [ ] P7-07 实现状态 `READY/RUNNING_COMMAND/RESETTING/EXITING/FAULTED`；只在 `READY` 检查空闲，执行中的文件系统命令不得被定时器中断。
- [ ] P7-08 提交任意非空输入（包括非法命令）或命令执行完成时更新活动时间；空行不续期。长命令结束后重新开始空闲计时。
- [ ] P7-09 超时后按顺序关闭文件、确保结束符和目录信息、刷缓冲/FAT/镜像；成功后清打开表、恢复根目录上下文、清未提交输入，再重置提示符或卸载退出。
- [ ] P7-10 为输入设置会话代次，重置时丢弃旧代次尚未提交的行；清理应用行缓冲和已排队的旧输入，测试不会在新会话执行残留命令。
- [ ] P7-11 清理失败进入 `FAULTED`，保留未完成的表项和脏数据；允许查询、重试刷新和安全退出流程，不假报“已保存”。
- [ ] P7-12 使用假时钟和输入替身测试阈值前后、非法命令续期、长命令、半行输入、连续多次重置、退出释放文件锁以及写回失败。

输入设计依据：JLine 提供 `NonBlockingReader` 和带超时的读取方式，适合将输入等待与超时检查交替执行。参见 [JLine 非阻塞输入](https://jline.org/docs/advanced/non-blocking-input/)。

实现重点：

- 不把阻塞的 `Scanner.nextLine()` 放到需要检查超时的循环中。
- 不通过反复新建线程再取消 `Future` 处理输入；取消并不保证控制台读取停止，容易残留多个输入消费者。
- 第一版由一个稳定的 Shell 输入循环负责终端；文件操作和重置仍通过同一串行核心执行路径。
- 批处理模式在到达 EOF 时清理退出，不用交互空闲策略等待下一条输入。
- 本计划中的“空闲”沿用既有约定：按已提交的非空输入计时，未按回车的半行不续期。超时提示需明确未提交输入会被清除。
- GUI 模式不应用 CLI 的空闲超时；GUI 内命令框也不自动关闭整个 GUI 会话。
- 无论 `reset` 还是 `exit`，都不能重新格式化或删除磁盘镜像。

规划的使用方式（代码完成后生效）：

```text
java -jar target/fatfs-lab-1.0.0.jar --cli --idle-timeout 600 --idle-action reset
java -jar target/fatfs-lab-1.0.0.jar --cli --idle-timeout 300 --idle-action exit
timeout status
timeout set 120
timeout action exit
timeout reset
```

**交付物：** CLI 启动入口、命令解析器、终端适配器、Shell 状态机、脚本样例、超时测试。

**验收：** 真实终端不输入时可以触发重置/退出；重置保留文件内容但清空打开表；长命令不被打断；自动化测试无需真实等待 10 分钟；退出后不遗留阻塞输入线程或被占用镜像。

### P8：Swing 图形界面与状态展示

**目标：** 完成指导书要求的图形界面，并能向教师展示算法运行状态。

任务：

- [ ] P8-01 建立 `MainFrame`，划分目录树、当前目录表、文件操作区、状态面板与日志区。
- [ ] P8-02 实现镜像新建/挂载/卸载入口；显式格式化已有镜像时显示目标路径与数据清空提示。
- [ ] P8-03 提供建立/列出/删除目录、创建/打开/读取/追加/关闭/删除/显示/改属性等操作的对话框或菜单。
- [ ] P8-04 复用 P7 命令解析与分发实现 GUI 内命令框，避免两套不同的参数和错误规则。
- [ ] P8-05 目录树显示真实层级；当前目录表显示名称、类型、属性、起始块、文件占块数。
- [ ] P8-06 显示 FAT 的 128 项和磁盘块状态：系统保留、空闲、目录、文件、坏块；同时使用文字标签，避免只靠颜色区分。
- [ ] P8-07 显示打开文件表的路径、模式、实际字节长度、读写指针；提供当前选中块的 64 字节十六进制查看。
- [ ] P8-08 后台串行执行磁盘命令；成功或失败后生成一致快照，回到 EDT 更新控件，避免界面长时间无响应。
- [ ] P8-09 实现忙碌态、明确中文错误和合法参数提示；核心故障时禁用修改入口，保持状态和诊断信息可见。
- [ ] P8-10 关闭窗口时复用安全关闭流程；刷新失败保持窗口和错误信息，不能直接丢失未完成状态。
- [ ] P8-11 在 Windows 实机核对中文路径、缩放、表格滚动、键盘焦点和常见操作流程；为报告保存代表性截图。

**交付物：** Swing 界面、视图模型、只读快照、操作截图、GUI 手工验收记录。

**验收：** 所有课程命令均有可操作入口；写跨块文件后 FAT 链和块数同步变化；删除文件后空间回收；界面线程不执行磁盘算法；同样输入在 GUI 与 CLI 中得到一致结果。

### P9：集成验收、打包和报告

**目标：** 把功能连成可重复演示的完整项目。

任务：

- [ ] P9-01 补全 `ConsistencyChecker`：检查系统保留项、坏块、环、越界、同一块被多文件引用、目录引用、孤立分配块和普通文件块数。
- [ ] P9-02 检查文件有效 `#` 与尾块位置，目录固定一块且保留字节为 0；损坏镜像输出诊断，不自动“修复”为新盘。
- [ ] P9-03 将第 8 节测试矩阵逐项关联到自动测试或有记录的手工验收，特别覆盖正常失败后的磁盘不变与 I/O 故障后的停止修改。
- [ ] P9-04 在独立进程中完成“写入→关闭→退出→重新启动→读回”，避免只在同一对象内验证缓存。
- [ ] P9-05 编写演示主函数/可执行脚本，覆盖 11 个课程命令；准备一次跨块写、一次目录满、一次权限失败和一次超时重置演示。
- [ ] P9-06 运行完整 `mvnw.cmd clean verify`，检查单元与集成测试报告，确认没有漏执行或被静默跳过的测试。
- [ ] P9-07 构建带依赖的可运行 JAR 与 `run-gui.cmd/run-cli.cmd`；验证 JLine 终端 provider 的资源在打包后仍可加载。
- [ ] P9-08 在 Java 21 和本机 JDK 25 启动验证，核对启动脚本不会误用 Java 8；脱离 IDE 执行一次完整演示。
- [ ] P9-09 编写使用说明、磁盘格式说明、模块职责、主要算法、测试结果和已知限制；整理截图及分工记录，供课程报告使用。
- [ ] P9-10 检查 Git 提交范围：源码、构建配置、文档与小型测试资源可提交；本地 `.skill/`、发现链接、运行镜像、日志和构建产物继续忽略。

**交付物：** 可运行 JAR、启动脚本、测试报告、演示脚本、用户手册、课程报告素材。

**验收：** 在规定环境按说明可以完成全部演示；R01～R16 均有证据；Shell 超时单独通过；尚未支持的功能和断电恢复边界清楚可见。

## 8. 测试矩阵与完成判定

测试优先核验可观察的行为和磁盘字节，不单纯重复实现中的每个分支。每项测试创建独立临时镜像，不接触正式 `data/fatfs.img`。

| 编号 | 场景 | 预期结果 | 阶段 / 需求 |
| --- | --- | --- | --- |
| T01 | 格式化并检查尺寸和布局 | 8192 字节；FAT 在 0/1；根目录在 2，8 个空项 | P2～P3 / R02～R06 |
| T02 | 读取和写入块 -1、128；挂载短镜像 | 明确失败，不影响其他文件 | P2 / R02、项目质量 |
| T03 | 两槽脏块替换、全部槽占用 | 正确写回；不额外建立第三槽 | P2 / R14 |
| T04 | 默认坏块与系统区 | 0～2、23、49 均不被分配 | P3 / R04～R05 |
| T05 | FAT 环、空闲后继、坏块后继、越界 | 检测失败，无死循环和负数下标 | P3 / R03、项目质量 |
| T06 | 编解码文件项和目录项 | 逐字节符合 8 字节格式；目录末字节为 0 | P4 / R06 |
| T07 | 多级路径、缺失父目录、中间段是文件 | 正常逐级检索；失败原因区分明确 | P4 / R07 |
| T08 | 目录第 8/9 项、根目录删除、非空删除 | 前 8 项成功；第 9 项和非法删除失败，不漏块 | P4 / R07、R13 |
| T09 | 创建同名文件、只读创建、打开表已满时创建 | 拒绝，FAT、目录和打开表均无变化 | P5 / R08～R09 |
| T10 | 第 5/6 个打开文件、同文件重复打开 | 最多 5 项；重复同模式不增加；不同模式冲突 | P5 / R08～R09 |
| T11 | 未打开文件读写、模式不符、只读写入 | 按规则自动打开或拒绝，失败不泄漏表项 | P5～P6 / R09～R11 |
| T12 | 未打开文件关闭，已打开文件删除/显示/改属性 | 关闭无操作；后三项拒绝 | P5 / R09、R12 |
| T13 | 内容长度 0/1/62/63/64/65/127/128/129 | 字节数据正确，块数含结束符所需空间 | P6 / R10～R11 |
| T14 | 63 字节后追加 1 字节，64 字节后继续追加 | 正确跨块与复用尾块，只有一个有效 EOF | P6 / R11 |
| T15 | 分次读取、读过 EOF、请求很大长度 | 指针推进正确；最多返回剩余数据，不超大分配 | P6 / R10 |
| T16 | 内容含 #、负写长、写长超过数组、整数溢出 | 修改前拒绝，保留原内容和指针 | P6 / 项目格式约定 |
| T17 | 仅剩不足以保存新 # 的空间 | 整次追加拒绝，旧文件与打开表保持原样 | P6 / R05、R11 |
| T18 | 真实写入阶段第 N 次 I/O 失败 | 不误报成功；进入 FAULTED 并阻止继续修改 | P6、P9 / 项目质量 |
| T19 | 全生命周期后退出并重启 | 文件数据、目录、属性和 FAT 持久化一致 | P6、P9 / 项目质量 |
| T20 | 输入非法命令、半行输入、EOF、旧会话残留输入 | 错误可读；超时可工作；旧输入不在新会话执行 | P7 / 用户追加 |
| T21 | 假时钟到阈值前/后，命令执行超过阈值 | 阈值前不重置；到期重置；不打断运行中命令 | P7 / 用户追加 |
| T22 | reset/exit 时有写方式打开文件 | 刷新成功后清理；reset 保留镜像，exit 释放锁 | P7 / 用户追加 |
| T23 | reset 清理失败 | 保留未完成状态，显示故障，不继续接受修改 | P7 / 用户追加 |
| T24 | GUI 操作与 CLI 同操作 | 返回语义和镜像结果一致，GUI 不受 CLI 超时影响 | P8 / R15、用户追加 |
| T25 | 中文宿主目录、Windows Terminal、脱离 IDE | 程序和镜像可正常访问，终端输入与退出正常 | P0、P9 / 交付质量 |
| T26 | 两个实例挂载同一镜像、交叉块占用 | 第二实例拒绝挂载；损坏引用能被一致性检查发现 | P9 / 项目质量 |

每个阶段的完成标准：任务清单已落实、对应测试通过、正常与失败场景均有结果、已知限制记录清楚。不能仅以“界面能打开”或“命令打印成功”判定完成。

## 9. 开发与验证命令约定

以下命令是工程建立后的目标工作流；当前目录尚没有 `pom.xml`、`mvnw.cmd` 或 JAR，不应将它们写成已执行成功的验证。

```powershell
# 运行单元测试
.\mvnw.cmd test

# 运行单元测试、集成测试与打包检查
.\mvnw.cmd clean verify

# 只定位某个单元测试类
.\mvnw.cmd -Dtest=FatTableTest test

# 打包后的 GUI 与 CLI
java -jar target/fatfs-lab-1.0.0.jar --gui
java -jar target/fatfs-lab-1.0.0.jar --cli --idle-timeout 600 --idle-action reset

# 自动演示：脚本执行结束后正常关闭和卸载
java -jar target/fatfs-lab-1.0.0.jar --script docs/demo-commands.txt
```

拟定的基础演示脚本：

```text
md /usr
md /usr/doc
create_file /usr/doc/a.tx 4
write_file /usr/doc/a.tx "hello" 5
close_file /usr/doc/a.tx
typefile /usr/doc/a.tx
open_file /usr/doc/a.tx read
read_file /usr/doc/a.tx 3
close_file /usr/doc/a.tx
change /usr/doc/a.tx 1
dir /usr/doc
delete_file /usr/doc/a.tx
rd /usr/doc
rd /usr
exit
```

脚本假定运行在新建的独立演示镜像上，包含全部 11 项课程命令。末尾删除文件依指导书仅检查是否打开；只读属性禁止写入，不额外推导出禁止删除的规则。

## 10. 分工与风险安排

### 10.1 单人或多人开发

单人按 P0～P9 推进。若按指导书建议组成 2～3 人小组，可在接口冻结后分工：

| 角色 | 主责 | 合作边界 |
| --- | --- | --- |
| A | 磁盘、双缓冲、FAT、格式化 | 给 B 提供稳定块接口和链接口 |
| B | 目录、路径、打开表、文件操作 | 给 C 提供统一服务接口和视图快照 |
| C | CLI、超时、Swing、演示与报告整合 | 使用测试替身提前开发交互，核心可用后集成 |

两人时由 B 同时承担 CLI，A/B 共同完成 GUI 和测试。每人维护所负责模块的测试，接口或磁盘格式变化需要同步说明。一个模块正确不等于组合正确，P9 仍需完整集成验收。

### 10.2 优先控制的风险

| 风险 | 尽早处理的方式 |
| --- | --- |
| Java 8/25 路径错配 | P0 统一 JDK，发布脚本校验运行版本 |
| 把字符数当字节数 | P1/P4/P6 固定编码，核心仅处理字节 |
| 64 字节边界没有空间保存 # | P6 提前计入结束符，加入 63/64/65 测试 |
| 双缓冲被多模块同时占用 | P2 定义槽位生命周期，P3/P6 检查嵌套读块 |
| 第 6 个创建产生孤立块 | P5 在分配前检查打开表容量 |
| 超时线程与写操作竞态 | P7 串行调度，运行中命令不触发清理 |
| 阻塞输入导致超时失效 | P0 验证限时读取，P7 使用输入适配器和假时钟 |
| UI 状态与真实磁盘不一致 | P8 每次操作后统一生成快照 |
| 对 I/O 故障夸大回滚能力 | 按第 5.5 节区分预检失败与实际写入故障 |
| 后期才发现不满足题目 | 每阶段对照 R01～R16 和测试矩阵 |

## 11. 最终交付核对表

- [ ] 提供 Java 源码、Maven 构建配置和明确的运行环境说明。
- [ ] 提供完整图形界面及 11 项课程命令入口。
- [ ] 磁盘、FAT、目录、打开表和双缓冲符合指导书格式。
- [ ] 能演示多级目录、跨块追加、文件权限、空间回收和重启读回。
- [ ] CLI 空闲 reset/exit、安全关闭和失败状态均通过验证。
- [ ] 自动测试实际运行，边界与异常场景均有证据。
- [ ] 可执行 JAR 脱离 IDE 启动，在约定 Java 环境完成演示。
- [ ] 报告包含需求、设计、数据结构、主要算法、测试、截图、分工和体会。
- [ ] 当前已知限制明确：绝对路径、每目录 8 项、追加写、保留 #、正常关闭持久化范围。
- [ ] `.skill/`、本地发现链接、运行镜像与构建产物未提交到 Git。

## 12. 开始编码时的第一批任务

先处理 P0 的 Java 路径错配，然后建立 Maven 骨架、`DiskLayout`、`BlockDevice` 和 `FileChannelDisk` 的块 I/O 测试。第一轮编码的可交付结果应是“能新建并重新打开 8192 字节镜像，正确读写指定块”，随后再进入双缓冲和 FAT。

与原始要求逐项核对时使用 [指导书要求提取](./题目五-指导书要求提取.md)。后续功能变化在本文和测试矩阵中记录，不反向改写指导书原文要求。
