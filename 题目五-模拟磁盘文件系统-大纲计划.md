# 题目五：模拟磁盘文件系统实现——大纲计划

> 历史方案说明（2026-09-22）：项目已选定 Java，当前开发安排见 [Java 开发方案与阶段任务](./题目五-Java开发方案与阶段任务.md)。本文保留早期调研与规划；原文要求及关闭行为、名称规则等差异，以 [指导书要求提取](./题目五-指导书要求提取.md) 为准。

> 依据：《操作系统分析与设计实习》指导书（第二版）题目五（正文第 17～24 页），并结合 GitHub 同类项目调研整理。  
> 当前阶段：需求梳理与实施规划，不包含代码实现。

## 1. 项目定位与目标

实现一个运行在 Windows 环境下的教学型模拟文件系统：

- 使用宿主机中的一个普通二进制文件模拟磁盘；
- 用两个 64 字节数组模拟磁盘读写缓冲区；
- 采用流式文件逻辑结构和 FAT 显式链接物理结构；
- 支持多级目录和绝对路径；
- 支持文件的创建、打开、关闭、读取、追加写入、删除、显示与属性修改；
- 支持目录的创建、列出和删除空目录；
- 通过图形界面调用上述功能并展示目录、文件、FAT 和磁盘块状态；
- 通过完整测试与课程设计报告说明数据结构、算法、调试过程和运行结果。

建议按指导书采用 2～3 人协作。如果是单人完成，也保留模块边界和分工记录，方便形成报告中的“工作内容与完成体会”。

## 2. 指导书硬性要求清单

### 2.1 模拟磁盘与 FAT

| 项目 | 指导书要求 | 计划中的实现约束 |
|---|---|---|
| 物理块数量 | 128 块 | 块号范围 `0..127` |
| 每块大小 | 64 字节 | 磁盘镜像固定为 `128 × 64 = 8192` 字节 |
| FAT 项数 | 128 项 | 每块对应 1 个 FAT 项 |
| FAT 项大小 | 1 字节 | FAT 共 128 字节，占用第 0、1 块 |
| 根目录 | 固定位置、固定大小 | 第 2 块，最多容纳 8 个 8 字节目录项 |
| 数据区 | FAT 和根目录之后 | 默认从第 3 块开始分配 |
| 空闲块 | FAT 值为 `0` | 分配时从数据区扫描，不得返回系统区块 |
| 文件尾 | 指导书用 `255` 代替 `-1` | FAT 链尾标记为 `0xFF` |
| 坏块 | 示例用 `254` | 预留 `0xFE`，不得分配 |

FAT 采用“块号即 FAT 下标”的显式链：若 `FAT[i] = j`，表示文件的下一块为 `j`。创建、扩展、删除文件时分别完成链的建立、延长和回收。

### 2.2 目录项布局

每个文件或目录登记项固定占 8 字节，因此一个目录块最多存放 8 项。

文件目录项：

| 字节 | 含义 |
|---|---|
| 0～2 | 文件基本名，3 字节 |
| 3～4 | 文件类型/扩展名，2 字节 |
| 5 | 属性位 |
| 6 | 起始盘块号 |
| 7 | 文件占用的盘块数 |

目录登记项：

| 字节 | 含义 |
|---|---|
| 0～2 | 目录名，3 字节 |
| 3～4 | 保留，填空格 |
| 5 | 目录属性 |
| 6 | 目录起始盘块号 |
| 7 | 保留，填 `0` |

属性字节按位解释：

- 第 0 位：只读文件；
- 第 1 位：系统文件；
- 第 2 位：可读、可写的普通文件；
- 第 3 位：目录；
- 第 4～7 位：保留。

示例：`00000011` 表示只读系统文件，`00000100` 表示普通可读写文件，`00001000` 表示目录。

文件名校验按指导书的 3+2 格式执行：基本名最多 3 个字符、类型最多 2 个字符；基本名和类型只接受合法字母/数字，并禁止把 `$`、`.`、`/` 当作名称字符。`.` 仅作为基本名与类型的分隔符，`/` 仅作为路径分隔符，`$` 用作空目录项标记。

### 2.3 目录结构与路径

- 支持根目录和子目录；
- 根目录固定占第 2 块；
- 每个子目录固定占 1 个盘块，因此最多含 8 个登记项；
- 文件和子目录登记项混合存放；
- 必须支持绝对路径，例如 `/abc/def/a.tx`；
- 路径解析从根目录开始，逐层读取目录块并查找同名目录项；
- 相对路径和可变长子目录属于增强项，不作为第一版验收条件。

### 2.4 已打开文件表

指导书给出最大打开文件数 `n = 5`。每项计划包含：

- 文件绝对路径；
- 文件属性；
- 起始盘块号；
- 文件实际字节长度；
- 打开方式（读或写）；
- 读指针：当前块号 + 块内偏移；
- 写指针：当前块号 + 块内偏移。

需明确区分：目录项第 7 字节记录“占用盘块数”，打开文件表记录“实际字节数”。实际文件结束由内容终止符 `#` 和内存中的长度共同判断。

### 2.5 必做文件命令

| 命令/功能 | 主要规则 |
|---|---|
| `create_file(path, attr)` | 父目录必须存在；同名文件不能重复；只读属性文件不能创建；分配至少 1 块；写入目录项并加入打开文件表 |
| `open_file(path, mode)` | 文件必须存在；写方式不能打开只读文件；重复打开不重复占表项；表满时失败 |
| `read_file(path, length)` | 文件不存在时失败；未打开则先按读方式打开；写方式打开的文件不得读取；从读指针开始跨 FAT 链读取，遇 `#` 或长度满足时停止 |
| `write_file(path, buffer, length)` | 未打开则按写方式打开；只允许从文件尾追加；按需逐块申请空间；处理最后一块未写满的情况 |
| `close_file(path)` | 文件必须已打开；写方式关闭时补写 `#`，更新目录项长度/块数并从打开文件表删除 |
| `delete_file(path)` | 文件必须存在且未打开；删除目录项；遍历 FAT 链并回收全部数据块 |
| `typefile(path)` | 文件存在且未打开时，从起始块沿 FAT 链逐块显示，遇 `#` 停止 |
| `change(path, attr)` | 文件存在且未打开时修改属性；必须验证属性组合是否合法 |

### 2.6 必做目录命令

| 命令/功能 | 主要规则 |
|---|---|
| `md(path)` | 父目录存在、无同名项、有空目录项和空闲盘块时建立目录 |
| `dir(path)` | 目录存在时逐项显示名称、类型、属性、起始块和长度等信息 |
| `rd(path)` | 根目录不得删除；仅允许删除空子目录；删除登记项并回收该目录块 |

### 2.7 磁盘 I/O 限制

- 不能把整个模拟磁盘一次性读入内存后直接操作；
- 每次只通过 64 字节块缓冲读取或写回指定盘块；
- 定义 `buffer1[64]`、`buffer2[64]` 模拟双缓冲；
- FAT 跨两个块，加载和写回时也必须通过块 I/O 接口；
- 所有修改需要及时持久化到磁盘镜像，重新启动程序后应能恢复原有目录与文件。

## 3. 关键设计决策

### 3.1 推荐技术路线

首选方案：`C++17 + Qt Widgets`。

- C++ 便于精确控制 1 字节字段、定长目录项和二进制块布局；
- Qt Widgets 适合完成 Windows 图形界面、树形目录、表格和操作对话框；
- 文件系统核心保持为独立 C++ 模块，不能把规则写死在界面事件中；
- 如果开发环境不具备 Qt，可切换为 `Java + Swing`，磁盘布局和模块接口不变。

在正式编码前先做一次环境验证：编译器版本、Qt/JDK 可用性、中文路径和文件名兼容性。

### 3.2 建议模块划分

1. `VirtualDisk`
   - 创建/加载 8192 字节磁盘镜像；
   - `readBlock(blockNo, buffer)`；
   - `writeBlock(blockNo, buffer)`；
   - 块号越界、短读短写和镜像损坏检查。

2. `FatManager`
   - FAT 初始化、加载和写回；
   - 查找空闲块；
   - 追加块、获取下一块、标记链尾；
   - 回收单块或整条 FAT 链；
   - 防止环、越界和重复释放。

3. `DirectoryEntry` / `DirectoryManager`
   - 8 字节目录项的序列化与反序列化；
   - 查找空目录项、同名检查、插入、修改和删除；
   - 列出目录内容；
   - 区分文件项和目录项。

4. `PathResolver`
   - 路径规范化与合法性校验；
   - 从根目录开始逐段解析绝对路径；
   - 返回目标项、父目录块、目录项下标；
   - 统一处理“不存在、路径中间项不是目录、重名”等错误。

5. `OpenFileTable`
   - 最多 5 个表项；
   - 查找、插入、删除和重复打开检查；
   - 保存读写方式及两个文件指针；
   - 提供界面可视化数据。

6. `FileSystemService`
   - 实现指导书要求的文件与目录命令；
   - 负责多个底层模块之间的一致性；
   - 所有操作返回统一的结果码和中文提示信息。

7. `CommandParser`
   - 支持命令行格式，便于自动测试；
   - 将命令解析成 `FileSystemService` 调用；
   - 图形界面和测试程序复用同一业务接口。

8. `MainWindow`
   - 左侧：目录树；
   - 中部：当前目录表格；
   - 右侧：FAT、打开文件表和盘块状态；
   - 下方：命令输入、日志和错误信息；
   - 操作按钮或右键菜单调用文件/目录命令。

### 3.3 初始化流程

1. 检查磁盘镜像是否存在且大小正确；
2. 若新建，则用 `0` 初始化全部盘块；
3. 初始化 128 项 FAT；
4. 将第 0、1、2 块标记为系统占用或链尾，保证不参与数据分配；
5. 将根目录第 2 块初始化为 8 个空目录项（首字节为 `$`）；
6. 清空内存中的已打开文件表；
7. 刷新 GUI 的目录、FAT 和磁盘状态视图。

建议给磁盘镜像增加一个很小的“格式版本/魔数”校验机制，但不能破坏指导书规定的 FAT 和根目录位置。若严格限制布局，可把版本信息放在单独的配置文件中。

### 3.4 一致性与失败回滚

重点避免“目录项已写入但 FAT 未完成”或“FAT 已分配但操作失败”形成泄漏：

- 建立文件/目录：先检查全部前置条件，再分配块，最后提交目录项；
- 写文件：记录本次新分配块，失败时回收新增部分并恢复原链尾；
- 删除文件：先验证文件未打开并完整读取 FAT 链，再删除目录项和释放块；
- 所有 FAT 链遍历最多访问 128 次，超过则判定为环或磁盘损坏；
- 系统区块、坏块和越界块永远不能进入普通文件链。

## 4. GitHub 开源项目调研与借鉴结论

### 4.1 [cadesalaberry/SimpleFS](https://github.com/cadesalaberry/SimpleFS)

- 技术：C，使用普通文件模拟磁盘，具有 FAT、根目录、文件描述符表和测试程序；
- 可借鉴：磁盘模拟层、FAT 下标代替内存指针、已打开文件表、API 与工具函数分层、自动测试入口；
- 与本题差异：只支持单级根目录，不能直接满足多级目录；
- 风险：仓库说明明确指出 `write` 存在越界错误，`read` 也未完成，因此只能借鉴结构，不能把读写代码当作正确答案；
- 使用原则：重新按 128×64B、8 字节目录项和本指导书命令独立实现，并自行补全边界测试。

### 4.2 [sidvishnoi/filesystem-simulation](https://github.com/sidvishnoi/filesystem-simulation)

- 技术：C++，MIT License；用二进制文件承载模拟文件系统，按扇区执行操作；
- 可借鉴：层次目录、路径命令、目录树展示、二进制持久化、扇区级读写的组织方式；
- 与本题差异：功能更复杂，包含删除恢复等扩展；其磁盘布局和分配方案不等同于本题的 1 字节 FAT；
- 使用原则：只借鉴路径解析、命令组织和扇区抽象，不引入恢复机制，不改变指导书指定布局。

### 4.3 [httpe/simple-fs](https://github.com/httpe/simple-fs)

- 技术：C；同时包含简化文件系统、FAT32、块 I/O、VFS、格式化工具和测试；
- 可借鉴：`block_io` 与文件系统逻辑分离、磁盘镜像、统一文件系统接口、格式化工具、测试模块；
- 与本题差异：FUSE 和 FAT32 远超课程题目范围，块大小和目录项也不同；
- 使用原则：借鉴模块边界与接口思想，不移植 FUSE、FAT32 长文件名、真实挂载或内核相关代码。

### 4.4 [ttsugriy/File-System-Simulator](https://github.com/ttsugriy/File-System-Simulator)

- 技术：Java；完整教学型文件系统模拟器，包含后备文件、块对象、打开文件描述符、目录和 `mkfs/mkdir/ls/cat` 等工具；
- 可借鉴：初始化流程、后备文件、逐块读写、错误码、用户命令说明、测试用小块磁盘；
- 与本题差异：主要采用 inode 和空闲位图，不是本题要求的 FAT；整体复杂度明显更高；
- 使用原则：借鉴测试方法、错误处理和用户手册结构，不照搬 inode/位图布局。

### 4.5 调研结论

没有发现一个同时满足“128×64B、两块 FAT、8 字节目录项、根目录固定第 2 块、绝对路径、打开文件表、双缓冲、指定命令和图形界面”的成熟仓库。最稳妥的方案是：

- 以指导书为唯一需求基准；
- 用 `SimpleFS` 理解 FAT 与打开文件表；
- 用 `filesystem-simulation` 理解层次目录和扇区级持久化；
- 用 `simple-fs` 设计块 I/O 与文件系统逻辑的边界；
- 用 `MOSS File System Simulator` 补强错误处理、测试和用户文档；
- 所有代码独立实现，并在报告中列出参考来源，避免直接拼接导致布局不一致或学术风险。

## 5. 分阶段实施计划

### 阶段 A：需求冻结与详细设计

- 列出全部命令、输入、输出、错误条件和状态变化；
- 确定磁盘镜像布局、FAT 特殊值、目录项字节布局；
- 明确文件长度、结束符 `#` 和块数的关系；
- 绘制模块调用关系图、文件创建流程图、读写流程图和删除回收流程图；
- 产出：需求清单、数据结构表、接口表和测试矩阵初稿。

### 阶段 B：虚拟磁盘与 FAT

- 完成镜像创建、加载、块读写和双缓冲；
- 完成 FAT 初始化、分配、链接、回收和持久化；
- 编写块边界、磁盘满、坏块、链尾、环检测测试；
- 产出：可独立测试的 `VirtualDisk` 与 `FatManager`。

### 阶段 C：目录项与绝对路径

- 完成 8 字节目录项编解码；
- 完成根目录/子目录初始化、查找、插入和删除；
- 完成绝对路径解析与合法性校验；
- 实现 `md`、`dir`、`rd`；
- 产出：可建立多级目录并重启恢复的版本。

### 阶段 D：文件与打开文件表

- 完成 `create_file`、`open_file`、`close_file`；
- 完成最多 5 项的已打开文件表；
- 完成权限检查、重复打开和表满检查；
- 产出：文件可建立、打开、关闭并在 GUI/调试输出中观察状态。

### 阶段 E：读写、删除与属性

- 完成跨块 `read_file`；
- 完成只追加的 `write_file`，处理最后一块剩余空间；
- 完成结束符 `#`、读写指针和实际长度维护；
- 完成 `delete_file`、`typefile`、`change`；
- 产出：所有必做命令可通过命令行脚本测试。

### 阶段 F：图形界面

- 建立目录树和当前目录表；
- 增加新建、打开、读、写、关闭、删除、改属性、建目录和删目录操作；
- 增加 FAT 表、盘块状态和已打开文件表视图；
- 将错误码转换为清晰提示；
- 产出：满足“图形界面”要求的可演示版本。

### 阶段 G：系统测试与报告

- 执行正常、边界、错误、持久化和一致性测试；
- 保存关键界面、FAT 变化、目录变化和读写结果截图；
- 整理源代码、可执行程序和课程设计报告；
- 按指导书报告结构完成需求分析、概要设计、详细设计、调试分析、用户说明、测试与运行结果。

## 6. 测试大纲

### 6.1 初始化与持久化

- 新建镜像大小恰为 8192 字节；
- 第 0～2 块不会被分配给普通文件；
- 关闭程序并重新启动后目录、内容、属性和 FAT 保持一致；
- 镜像尺寸不正确或 FAT 链损坏时给出错误，而不是越界访问。

### 6.2 目录测试

- 在根目录建立子目录、在子目录继续建立目录；
- 路径不存在、中间项不是目录、同名、非法名称均失败；
- 一个目录写满 8 项后继续创建应失败；
- 非空目录和根目录不能删除；
- 删除空目录后，其盘块和父目录项均被回收。

### 6.3 文件基本操作

- 创建、重复创建、父目录不存在、非法属性；
- 读方式和写方式打开；只读文件拒绝写打开；
- 同一文件重复打开；同时打开第 6 个文件；
- 未打开读/写时按规则自动打开；
- 未打开文件执行关闭应失败。

### 6.4 读写边界

- 0 字节、1 字节、63 字节、64 字节、65 字节和多块写入；
- 多次追加写入后内容顺序正确；
- 跨 FAT 链读取，读指针正确推进；
- 读取长度大于剩余内容时在 `#` 处停止；
- 磁盘空间不足时不留下孤儿块或损坏原文件；
- 写方式打开的文件拒绝读取，读方式打开的文件拒绝写入。

### 6.5 删除、显示和属性

- 打开的文件不能删除、显示或改属性；
- 删除多块文件后 FAT 链全部归零；
- `typefile` 的输出与写入内容一致；
- 改为只读后写操作失败；改回普通文件后可继续追加。

### 6.6 一致性检查器（建议增加）

提供仅用于调试/测试的 `fsck` 检查：

- FAT 链是否越界、成环或指向系统块；
- 两个目录项是否共享同一数据块；
- 已分配块是否都能从某个目录项到达；
- 空闲块是否被目录项引用；
- 目录项记录的块数是否与 FAT 链长度一致。

## 7. 报告大纲

指导书规定报告主体包含以下六部分：

1. 需求分析
   - 输入形式和值域；
   - 输出形式；
   - 可实现功能；
   - 正确输入、错误输入及预期结果。

2. 概要设计
   - 抽象数据类型和核心数据结构；
   - 主程序流程；
   - 模块层次和调用关系图。

3. 详细设计
   - 磁盘布局和目录项字节布局；
   - FAT 分配/回收算法；
   - 路径解析算法；
   - 各命令伪代码；
   - 主要函数调用关系图。

4. 调试分析
   - 遇到的问题、定位过程和解决方案；
   - 时间复杂度分析与改进设想；
   - 设计与实现中的经验体会。

5. 用户使用说明
   - 环境与启动方式；
   - 磁盘初始化；
   - 图形界面操作步骤；
   - 命令格式与示例；
   - 常见错误提示。

6. 测试与运行结果
   - 测试用例表；
   - 输入、预期输出和实际输出；
   - 关键运行界面截图；
   - FAT、目录和已打开文件表状态截图。

报告首页按指导书附成绩单/成员分工表，并填写班级、学号、姓名、题目、工作量比例和个人体会。报告中只附关键源代码，不必粘贴全部代码。

## 8. 建议里程碑（两周版本）

| 时间 | 目标 | 验收物 |
|---|---|---|
| 第 1 天 | 冻结需求和磁盘布局 | 数据结构与接口说明 |
| 第 2～3 天 | 虚拟磁盘、双缓冲、FAT | 块 I/O 与 FAT 单元测试通过 |
| 第 4～5 天 | 目录项、路径解析、目录命令 | 多级目录和持久化通过 |
| 第 6～7 天 | 创建、打开、关闭、打开文件表 | 文件生命周期测试通过 |
| 第 8～9 天 | 读取、追加写入、删除、属性 | 跨块读写与回收测试通过 |
| 第 10～11 天 | GUI 与状态可视化 | 可演示程序 |
| 第 12 天 | 错误与边界测试、`fsck` | 测试记录和缺陷清单 |
| 第 13～14 天 | 修复、截图、报告和打包 | 源码、可执行程序、报告 |

## 9. 暂不纳入第一版的增强项

- 相对路径和当前工作目录；
- 任意长度子目录；
- 文件中间位置修改和 `seek`；
- 并发访问与锁；
- 删除恢复、日志和崩溃恢复；
- FAT12/FAT16/FAT32 兼容；
- 挂载为真实操作系统文件系统；
- Unicode 长文件名。

这些功能可在必做项全部通过后选择一项扩展，不能挤占核心功能和报告时间。

## 10. 当前结论与下一步

本题的最大风险集中在四处：

1. 8 字节目录项、文件实际字节长度和 `#` 结束符之间的一致性；
2. 跨块追加写、FAT 链延长以及磁盘满时的回滚；
3. 多级绝对路径解析和固定 8 项目录容量；
4. 不把整个镜像常驻内存，同时保证 FAT、目录项和数据块持久化一致。

下一步应先确定实现语言与 GUI 技术，然后产出“详细设计说明”：给出 C++/Java 数据结构、函数签名、错误码、磁盘镜像字节布局和各命令伪代码，再开始编码。

## 参考资料

- 本地指导书：《操作系统分析与设计实习》指导书（第二版），题目五及课程设计报告规范。
- [cadesalaberry/SimpleFS](https://github.com/cadesalaberry/SimpleFS)
- [sidvishnoi/filesystem-simulation](https://github.com/sidvishnoi/filesystem-simulation)
- [httpe/simple-fs](https://github.com/httpe/simple-fs)
- [ttsugriy/File-System-Simulator](https://github.com/ttsugriy/File-System-Simulator)
